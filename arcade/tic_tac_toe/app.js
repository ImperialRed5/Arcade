const gameBoard = document.querySelector( '#game-board' );
const gameBoardCells = document.querySelectorAll( '.cell' );
const gameBoardCellsArray = Array.from( gameBoardCells );
const gameBoardCellsArrayLength = gameBoardCellsArray.length;

  // function createGameBoard() {
  //   for ( let i = 0; i < gameBoardCellsArrayLength; i++ ) {
  //     gameBoardCellsArray[ i ].setAttribute( 'data-cell-index', i );
  //   }
  // }

